# <%= packageName %>

由 [create-applet](https://github.com/kangduu/applet) 生成的**最小占位工程**（CLI MVP）。

## 已选平台

<%= platformList %>

## 说明

- 当前模板为 **minimal**，用于验证脚手架「交互 + EJS 渲染 + 落盘」流程。
- **basic-react**（Taro 4 + React + weapp/h5）见仓库下一迭代 `pm/backlog.md`。

## 本地命令

```bash
<%= packageManager %> install
<%= packageManager %> run dev
```

---
Generated <%= year %>
